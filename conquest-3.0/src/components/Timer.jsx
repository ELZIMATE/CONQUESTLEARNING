import { useEffect, useState } from "react"
import { addtimer } from "../hooks/backendapi/Featuresapi"
import ClockSetter from "./Screens/ClockSetter"

const Timer = ({ clockset, setClockSet, workTime, setWorkTime, socialTime, setSocialTime, leisureTime, setLeisureTime, fitnessTime, setFitnessTime, isRunning, running, category, setCategory, user, entry }) => { //timer function/component is starting up with hooks from useFeats imported





return(
    <>

    
    </>
)

}


export default Timer
